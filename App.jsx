const { useState, useEffect } = require('react');
const { Box, Color } = require('ink');

const defaultSizes = [48, 72, 96, 144, 168, 192, 256, 512];

function App({ iconPath, sizes = defaultSizes }) {}

module.export = App;
